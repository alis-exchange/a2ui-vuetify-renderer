name: Publish to npm

on:
    push:
        tags:
            - "v*" # Triggers when a tag starting with 'v' is pushed

jobs:
    changelog:
        runs-on: ubuntu-latest

        steps:
            # Step 1: Checkout the repository with full history
            - name: Checkout repository
              uses: actions/checkout@v5
              with:
                  fetch-depth: 0 # Ensure full commit history is fetched, including tags

            # Step 2: Find the branch associated with the commit
            - name: Determine the branch
              id: extract_branch
              run: |
                  # Try to find the branch based on commit history
                  BRANCH=$(git for-each-ref --format='%(refname:short)' $(git rev-parse --verify HEAD) | grep 'heads/' | head -n 1 | sed 's|heads/||')
                  echo "Branch: $BRANCH"
                  if [ -z "$BRANCH" ]; then
                    echo "No branch found, defaulting to 'main'"
                    BRANCH="main"  # Fallback to 'main' if no branch is found
                  fi
                  echo "branch=$BRANCH" >> $GITHUB_OUTPUT

            # Step 3: Set up pnpm (needed for generate:catalog: tsx + script deps)
            - name: Set up pnpm
              uses: pnpm/action-setup@v5
              with:
                  version: 9

            # Step 4: Set up Node.js with pnpm cache
            - name: Set up Node.js
              uses: actions/setup-node@v5
              with:
                  node-version: "22"
                  cache: "pnpm"

            # Step 5: Install project dependencies (tsx, zod, etc. for catalog generation)
            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            # Step 6: Install conventional-changelog-cli
            - name: Install conventional-changelog-cli
              run: |
                  npm install -g conventional-changelog-cli

            # Step 7: Generate changelog
            - name: Generate changelog
              run: |
                  conventional-changelog -p angular -i CHANGELOG.md -s -r 0

            # Step 8: Update package.json version from the tag
            - name: Update package.json version
              run: |
                  VERSION="${GITHUB_REF#refs/tags/v}"
                  npm pkg set "version=${VERSION}"

            # Step 9: Regenerate catalog JSON (scripts/generate-catalog.ts via tsx)
            - name: Generate catalog
              run: pnpm generate:catalog

            # Step 10: Commit and push changelog, version, and catalog to the determined branch
            - name: Commit and push release artifacts
              run: |
                  git config --global user.name "github-actions[bot]"
                  git config --global user.email "github-actions[bot]@users.noreply.github.com"
                  git add CHANGELOG.md package.json catalog/vuetify-catalog.json
                  git commit -m "chore: release ${GITHUB_REF#refs/tags/}"

                  # Push to the determined branch
                  git push origin HEAD:${{ steps.extract_branch.outputs.branch }}
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    publish:
        needs: changelog
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v5

            - name: Set up pnpm
              uses: pnpm/action-setup@v5
              with:
                  version: 9

            - name: Set up Node.js
              uses: actions/setup-node@v5
              with:
                  node-version: "22"
                  registry-url: "https://registry.npmjs.org"
                  scope: "@alis-build"
                  cache: "pnpm"

            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            - name: Run tests
              run: pnpm test

            - name: Generate catalog
              run: pnpm generate:catalog

            - name: Build
              run: pnpm build

            - name: Extract version number
              id: extract_version
              shell: bash
              run: |
                  VERSION="${GITHUB_REF#refs/tags/v}"
                  echo "VERSION=${VERSION}" >> $GITHUB_OUTPUT

            - name: Update package.json version
              run: npm version ${{ steps.extract_version.outputs.VERSION }} --no-git-tag-version

            - name: Publish to npm
              run: npm publish --access public
              env:
                  NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
